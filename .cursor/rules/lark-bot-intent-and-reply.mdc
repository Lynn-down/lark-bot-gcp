---
description: Lark 机器人意图识别模糊化与回复随机化；新增功能时均遵循
alwaysApply: true
globs: app.py
---

# Lark 机器人：意图识别 + 回复随机化

修改 `app.py` 或新增类似逻辑时，必须遵循：

## 1. 意图识别模糊化
- 不用单一固定句式，用**多组近义关键词**覆盖用户可能的各种说法
- 示例：阅读文档 → 读、看看、查看、前文、上文、文档、链接、帮我读、你读一下… 等
- 内化/技能 → 内化、学、记住、吸收、纳入、记下来… 等
- 提炼/总结 → 提炼、提取、总结、概括、要点、核心、关键信息… 等

## 2. 回复自行重生成（随机组合）
- 禁止固定 2～3 条轮换，每次回复都通过**多段随机组合**生成
- 将回复拆成开头/中间/结尾若干片段，每段 5～8+ 个选项，用 `_pick()` 随机选后拼接
- 示例：`_pick(openers) + _pick(middles) + dynamic_content + _pick(closers)`，组合数 = 各段选项数之积

## 3. 文档来源优先级
- 当前消息中的链接 → 引用消息 → 最近聊天记录
- 用户可直接发链接+「读一下」，或引用链接再说「看看」
