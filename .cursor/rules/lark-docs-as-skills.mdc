---
description: Use Lark/飞书 cloud docs shared in chat as internal knowledge—summarize, answer from them, generate docs from templates
alwaysApply: true
---

# Lark 云文档内化为技能

当用户在本项目中分享「飞书 / Lark 云文档」内容（粘贴、链接说明或导出文本）时，按以下方式处理：

## 1. 阅读与总结
- 通读文档内容，提取结构、要点、关键信息。
- 应要求给出简短总结或结构化摘要（如按章节/条款）。

## 2. 内化为可检索知识
- 将文档中的事实、流程、模板、规范等视为可复用的「技能」。
- 当用户或他人问及与这些文档相关的问题时，从文档中提取信息并准确回答；若文档中无相关内容，明确说明并建议可补充来源。

## 3. 按模板生成新文档
- 若文档中包含可复用模板（如申请单、报告格式、规范模板），在用户提供「换一批信息」或「按模板生成新文档」时：
  - 识别模板中的占位项或可变部分；
  - 根据用户提供的新信息填充，生成新的文档草稿；
  - 保持原模板的格式与结构，仅替换或补充指定内容。

## 4. 与 Lark 机器人的衔接
- 若用户希望将某文档的能力做到「别人在 Lark 里问相关问题也能答」，则在本项目中预留或实现：把文档要点/FAQ 写入 `docs_knowledge/` 或配置，供 `app.py` 中的回复逻辑调用（例如关键词→引用文档中的句子或链接）。
