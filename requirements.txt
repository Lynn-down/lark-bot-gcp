# ── 飞书开放平台 Python SDK ──────────────────────
lark-oapi>=1.2.0

# ── Web 服务 ────────────────────────────────────
flask>=3.0.0

# ── 环境变量 ────────────────────────────────────
python-dotenv>=1.0.0

# ── HTTP 请求（飞书 REST API + LLM API）─────────
requests>=2.31.0

# ── Word 文档生成（真实 DOCX 模板填充）──────────
python-docx>=0.8.11

# ── 日期计算（合同到期日 relativedelta）─────────
python-dateutil>=2.8.0
